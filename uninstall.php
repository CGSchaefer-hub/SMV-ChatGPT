<?php

if (!defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

global $wpdb;

/*
|--------------------------------------------------------------------------
| Tabelle löschen
|--------------------------------------------------------------------------
*/

$table = $wpdb->prefix . 'course_participants';
$wpdb->query("DROP TABLE IF EXISTS $table");

/*
|--------------------------------------------------------------------------
| Optionen löschen
|--------------------------------------------------------------------------
*/

delete_option('cm_send_confirmation_mail');

/*
|--------------------------------------------------------------------------
| Rolle entfernen
|--------------------------------------------------------------------------
*/

remove_role('kursleiter');

/*
|--------------------------------------------------------------------------
| Post-Type Daten bleiben bewusst erhalten
| (damit Inhalte nicht automatisch gelöscht werden)
|--------------------------------------------------------------------------
*/
